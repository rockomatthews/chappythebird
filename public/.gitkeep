# Drop assets here. The site looks for `public/chappyTheBirdLogo.png` —
# if you upload a new logo with that exact filename, the site picks it up
# automatically. If you use a different filename, ping me and I'll update
# the references.
